# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Contest run outputs (keep the .gitkeep placeholder)
source/outputs/*.json

# Root-level local run artifacts (result files + run logs) — generated, never commit
result*.json
*.log

# Local contest materials — questions/attachments/sensitive data, NOT for the remote
publish/
real_questions*.json

# Local-only model endpoints (e.g. DashScope test key) — never commit secrets
.env.dashscope
.env.local
