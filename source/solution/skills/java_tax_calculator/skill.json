{
  "name": "java_tax_calculator",
  "description": "Primary path: repair the buggy Java tax source with the model (reasoning off), compile with javac, validate against the worked examples, run the hidden salaries, and prefix the real java -version. Falls back to a deterministic Python re-implementation when the JDK/gateway is missing or the deadline is near.",
  "entrypoint": "scripts/run.py",
  "timeout_seconds": 480,
  "input_schema": {
    "type": "object",
    "properties": {
      "task_description": {
        "type": "string",
        "description": "The question text, including the hidden salary cases."
      },
      "source_file": {
        "type": "string",
        "description": "Path to the declared Java source file. Relative paths resolve against the question directory."
      }
    },
    "required": [
      "task_description"
    ],
    "additionalProperties": true
  }
}
