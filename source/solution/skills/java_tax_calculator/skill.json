{
  "name": "java_tax_calculator",
  "description": "Repair the buggy Java tax source with the model, compile and validate against the worked examples, then compute the hidden salary outputs with a java-version prefix (python fallback when no JDK).",
  "entrypoint": "scripts/run.py",
  "timeout_seconds": 240,
  "input_schema": {
    "type": "object",
    "properties": {
      "task_description": {
        "type": "string",
        "description": "The question text, including the hidden salary cases."
      },
      "source_file": {
        "type": "string",
        "description": "Path to the declared Java source file. Relative paths resolve against the question directory."
      }
    },
    "required": [
      "task_description"
    ],
    "additionalProperties": true
  }
}
